# Configurações do MQTT Gateway

# Endereço do broker MQTT (0.0.0.0 escuta em todas as interfaces)
MQTT_BROKER=0.0.0.0

# Porta do broker MQTT (padrão: 1883)
MQTT_PORT=1883

# Diretório onde os dados serão salvos
DATA_DIR=data
